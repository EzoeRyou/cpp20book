## constリファレンス修飾子のついたメンバーへのポインターの修正

C++17までは以下のコードがエラーとなっていた。

~~~cpp
struct X { void foo() const&; };

X{}.foo();        // OK
(X{}.*&X::foo)(); // エラー
~~~

これはconstなリファレンス修飾子のエラーとなるべきコードではないが、規格の文面上の問題でC++17まではエラーになっていた。C++20で修正された。
